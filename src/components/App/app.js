import React, { Fragment } from 'react';
import SearchBar from '../Search-bar';
import MainPage from '../Main-page';

const App = () => (
    <Fragment>
        <SearchBar />
        <MainPage />
    </Fragment>
);

export default App;
